# Placeholder. Full release pipeline uses cargo-dist + release-plz.
# Set up per their installers:
#   cargo install cargo-dist && cargo dist init
#   cargo install release-plz
# Then commit the generated workflows. Tracked separately from the v1 MVP.
name: release
on:
  push:
    tags: ['v*']
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "release pipeline TBD — see cargo-dist"
