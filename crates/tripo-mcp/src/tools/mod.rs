//! Tool implementations are inlined into `TripoServer` via `#[tool_router]`.
//!
//! This module is a placeholder so that future tool groupings (if any) can land here
//! without reshuffling the crate layout. All tool methods currently live on the
//! `TripoServer` impl in `server.rs`.
